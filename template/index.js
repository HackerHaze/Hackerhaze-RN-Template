/**
 * @format
 */

const {initializeApp} = require('./src/app');

// import {AppRegistry} from 'react-native';
// import App from './App'; // Make sure to import your main App component
// import {name as appName} from './app.json';

// AppRegistry.registerComponent(appName, () => App);
// import {Navigation} from 'react-native-navigation';

// import registerScreens from './src/shared/navigation/registerScreens';
// import {rootNavigation} from './src/shared/navigation/navigation';

// registerScreens();
// Navigation.events().registerAppLaunchedListener(() => rootNavigation());
initializeApp();
